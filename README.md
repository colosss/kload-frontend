## 🌐🎨 Frontend

**Frontend** — публичный веб-клиент форума, предоставляющий пользовательский интерфейс для взаимодействия с микросервисной платформой.
Приложение доступно любому пользователю по URL **kload.ru** и является визуальной точкой входа в систему.

Frontend взаимодействует с backend **исключительно через API Gateway**, не имея прямого доступа к внутренним сервисам, и полностью изолирован от их реализации.

Frontend является частью проекта **Microservices_Site**.

---

### 🔧 Зона ответственности

* отображение пользовательского интерфейса форума;
* навигация между страницами и разделами приложения;
* работа с формами (вход, регистрация, создание контента);
* клиентская валидация пользовательских данных;
* обработка и отображение ошибок backend;
* управление авторизацией пользователя;
* хранение и обновление access / refresh token (через cookies);
* взаимодействие с backend по HTTP (через Gateway);
* переиспользуемые UI-компоненты страниц.

---

### 🧩 Роль в архитектуре

* является **публичным клиентом** платформы;
* предоставляет UI для конечных пользователей;
* взаимодействует **только с Gateway**, без прямых вызовов backend-сервисов;
* не содержит бизнес-логики — вся логика обрабатывается на backend;
* отдаётся пользователю через **Nginx**;
* использует Nginx как:

  * reverse proxy;
  * точку интеграции с Gateway;
* масштабируется независимо от backend-части.

---

### 🚀 Технологический стек и зависимости

| Компонент      | Назначение                              |
| -------------- | --------------------------------------- |
| **React**      | Построение пользовательского интерфейса |
| **TypeScript** | Типизация и безопасность кода           |
| **JavaScript** | Базовая логика браузерного исполнения   |
| **Vite**       | Сборка и dev-сервер                     |
| **Axios**      | HTTP-взаимодействие с Gateway           |
| **ReactDOM**   | Рендеринг React-приложения              |
| **Nginx**      | Отдача фронта и проксирование запросов  |
| **Docker**     | Контейнеризация и деплой                |

---

### 🔀 Маршрутизация и навигация

Frontend реализует клиентскую маршрутизацию (SPA):

* страницы форума;
* страницы авторизации и регистрации;
* пользовательские профили;
* просмотр и работа с постами.

Все внешние HTTPS-запросы из браузера направляются:

```
Browser (HTTPS) → Nginx (TLS termination) → Gateway → Backend services
```

---

### 🔐 Авторизация и безопасность

* фронт **не реализует собственную систему безопасности**;
* авторизация полностью делегирована backend-части;
* используется:

  * access token;
  * refresh token;
* refresh token хранится в cookies;
* обновление access token происходит автоматически;
* прямой доступ к backend-сервисам отсутствует.

---

### 🛡️ Архитектурные примечания

* Frontend не содержит бизнес-логики форума;
* все проверки прав и доступов выполняются на backend;
* приложение является:

  * чисто информационным форумом;
  * без платёжных систем;
  * без товаров и коммерческих операций;
* UI реализован **полностью кастомно**, без сторонних UI-библиотек;
* компоненты страниц переиспользуются локально внутри проекта.

---

### 📌 Итог

Frontend — это лёгкий, изолированный и масштабируемый клиент, обеспечивающий удобный доступ пользователей к функционалу форума, сохраняя строгую границу ответственности между UI и backend-логикой.

---